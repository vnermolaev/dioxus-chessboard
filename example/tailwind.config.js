module.exports = {
    mode: "all",
    content: [
        // include all rust, html and css files in the src directory
        "./**/*.{rs,html}"
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}